<%- include ('../partials/header'); %>

<main class="main container">   
    <div class="row">
        <% data.forEach(item => { %>
            <div class="col-md-4">
                    <h2><%= item. %></h2>
                    <p> <%= item.%></p>
                    <p> <%= item. %></p>
                    <p> <%= item. %></p>
                    <hr>
                </a>
            </div>
        <% }) %>
    </div>
</main>

<%- include ('../partials/header'); %>